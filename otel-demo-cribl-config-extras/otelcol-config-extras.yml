# SPDX-License-Identifier: Apache-2.0

# extra settings to be merged into OpenTelemetry Collector configuration
# do not delete this file

## Example configuration for sending data to your own OTLP HTTP backend
## Note: the spanmetrics exporter must be included in the exporters array
## if overriding the traces pipeline.
##

## {http/s}://{$workergroupname}.{$workspacename}.{$organizationId}.cribl.cloud:{$port} 
## the port you configured in the Cribl OpenTelemetry Source
 exporters:
   otlphttp/cribl:
     endpoint: "https://default.main.best-goat-ever.cribl.cloud:20009"

 service:
   pipelines:
     traces:
       exporters: [otlp, debug, spanmetrics, otlphttp/cribl]
     logs:
       exporters: [opensearch, debug,  otlphttp/cribl]
     metrics:
       exporters: [otlphttp/prometheus, debug, otlphttp/cribl]